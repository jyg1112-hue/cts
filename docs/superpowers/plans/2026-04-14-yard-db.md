# yard.html DB 연동 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** yard.html 시뮬 설정을 Supabase DB에 저장하고, 선석 스케줄 데이터를 DB에서 불러와 자동 반입 계산이 정상 동작하도록 한다.

**Architecture:** `yard_sim_settings (mode TEXT PK, data JSONB)` 테이블 신규 생성. FastAPI에 `GET/PUT /api/yard-sim?mode=` 2개 엔드포인트 추가. yard.html은 페이지 로드 시 async IIFE로 두 모드 설정과 스케줄을 병렬 fetch해 `_simCache`, `_scheduleCache` 전역 변수에 저장. 이후 `loadSimSettings()`, `saveSimSettings()`, `calcAutoInbound()`는 캐시를 읽어 동기 유지.

**Tech Stack:** Python 3.11, FastAPI, psycopg3, PostgreSQL(Supabase), vanilla JS fetch API

---

## 파일 변경 목록

| 파일 | 작업 |
|------|------|
| `backend/main.py` | `yard_sim_settings` 테이블 생성, `GET/PUT /api/yard-sim` 엔드포인트, auth 미들웨어 화이트리스트 |
| `tests/test_yard_sim_api.py` | 신규 — API 단위 테스트 4개 |
| `yard.html` | 캐시 변수 추가, `loadSimSettings`/`saveSimSettings`/`calcAutoInbound` 수정, async IIFE 초기화 |

---

### Task 1: 백엔드 — `yard_sim_settings` 테이블 + `/api/yard-sim` 엔드포인트

**Files:**
- Modify: `backend/main.py`
- Create: `tests/test_yard_sim_api.py`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_yard_sim_api.py` 파일 생성:

```python
from __future__ import annotations

from unittest.mock import patch

import pytest
from fastapi.testclient import TestClient


@pytest.fixture()
def client():
    from backend.main import app
    return TestClient(app, raise_server_exceptions=False)


class TestYardSimApi:
    def test_get_returns_null_when_no_data(self, client):
        with patch("backend.main._db_get_yard_sim", return_value=None):
            res = client.get("/api/yard-sim?mode=overall")
        assert res.status_code == 200
        assert res.json() is None

    def test_get_returns_data_when_exists(self, client):
        mock_data = {"selfYard": {"capa": 248400}, "rentYard": {"capa": 172800}}
        with patch("backend.main._db_get_yard_sim", return_value=mock_data):
            res = client.get("/api/yard-sim?mode=import")
        assert res.status_code == 200
        assert res.json()["selfYard"]["capa"] == 248400

    def test_get_invalid_mode_returns_400(self, client):
        res = client.get("/api/yard-sim?mode=invalid")
        assert res.status_code == 400

    def test_put_saves_data(self, client):
        payload = {"selfYard": {"capa": 248400}, "rentYard": {"capa": 172800}}
        with patch("backend.main._db_save_yard_sim") as mock_save:
            res = client.put("/api/yard-sim?mode=overall", json=payload)
        assert res.status_code == 200
        mock_save.assert_called_once_with("overall", payload)

    def test_put_invalid_mode_returns_400(self, client):
        res = client.put("/api/yard-sim?mode=bad", json={})
        assert res.status_code == 400
```

- [ ] **Step 2: 테스트 실행해 실패 확인**

```bash
cd /Users/jo/Desktop/프로젝트
python3 -m pytest tests/test_yard_sim_api.py -v
```

Expected: `AttributeError` 또는 404 — `_db_get_yard_sim`, `/api/yard-sim` 미정의

- [ ] **Step 3: DB 헬퍼 함수 추가**

`backend/main.py`에서 `_ALLOWED_ITEM_TABLES` 상수 바로 아래(45번째 줄 이후)에 추가:

```python
_ALLOWED_SIM_MODES = frozenset({"overall", "import"})


def _ensure_yard_sim_table() -> None:
    """yard_sim_settings 테이블이 없으면 생성."""
    dsn = _db_dsn()
    if not dsn:
        return
    import psycopg
    with psycopg.connect(dsn) as conn:
        with conn.cursor() as cur:
            cur.execute("""
                CREATE TABLE IF NOT EXISTS yard_sim_settings (
                    mode TEXT PRIMARY KEY,
                    data JSONB NOT NULL,
                    updated_at TIMESTAMPTZ DEFAULT NOW()
                )
            """)
        conn.commit()


def _db_get_yard_sim(mode: str) -> dict | None:
    """해당 모드 SIM 설정 반환. 없으면 None."""
    dsn = _db_dsn()
    if not dsn:
        return None
    import psycopg
    with psycopg.connect(dsn) as conn:
        with conn.cursor() as cur:
            cur.execute("SELECT data FROM yard_sim_settings WHERE mode = %s", (mode,))
            row = cur.fetchone()
            return row[0] if row else None


def _db_save_yard_sim(mode: str, data: dict) -> None:
    """해당 모드 SIM 설정 저장 (upsert)."""
    dsn = _db_dsn()
    if not dsn:
        return
    import psycopg
    with psycopg.connect(dsn) as conn:
        with conn.cursor() as cur:
            cur.execute(
                """
                INSERT INTO yard_sim_settings (mode, data, updated_at)
                VALUES (%s, %s, NOW())
                ON CONFLICT (mode) DO UPDATE SET data = EXCLUDED.data, updated_at = NOW()
                """,
                (mode, json.dumps(data, ensure_ascii=False)),
            )
        conn.commit()
```

- [ ] **Step 4: `debug_startup_trace`에 테이블 생성 호출 추가**

`backend/main.py`의 `debug_startup_trace` 함수에서 `_ensure_schedule_banchu_tables()` 바로 아래에 추가:

```python
    _ensure_yard_sim_table()
```

결과 (1589~1590번째 줄 근처):
```python
@app.on_event("startup")
def debug_startup_trace() -> None:
    _ensure_schedule_banchu_tables()
    _ensure_yard_sim_table()
    # region agent log
    ...
```

- [ ] **Step 5: `/api/yard-sim` GET/PUT 엔드포인트 추가**

`backend/main.py`에서 `/api/banchu` PUT 엔드포인트 바로 뒤(`/api/config` 앞)에 추가:

```python
_ALLOWED_SIM_MODES_SET = frozenset({"overall", "import"})


@app.get("/api/yard-sim")
def get_yard_sim(mode: str) -> JSONResponse:
    if mode not in _ALLOWED_SIM_MODES_SET:
        raise HTTPException(status_code=400, detail="mode는 'overall' 또는 'import'여야 합니다.")
    try:
        data = _db_get_yard_sim(mode)
    except Exception:
        data = None
    return JSONResponse(data)


@app.put("/api/yard-sim")
async def put_yard_sim(request: Request, mode: str) -> JSONResponse:
    if mode not in _ALLOWED_SIM_MODES_SET:
        raise HTTPException(status_code=400, detail="mode는 'overall' 또는 'import'여야 합니다.")
    try:
        data = await request.json()
        if not isinstance(data, dict):
            raise HTTPException(status_code=400, detail="객체 형식이어야 합니다.")
        _db_save_yard_sim(mode, data)
    except HTTPException:
        raise
    except Exception as e:
        raise HTTPException(status_code=500, detail=f"저장 실패: {e}")
    return JSONResponse({"ok": True})
```

> **참고:** `_ALLOWED_SIM_MODES`는 Step 3에서 이미 추가했으므로 `_ALLOWED_SIM_MODES_SET`은 중복 정의임. 실제로는 Step 3에서 정의한 `_ALLOWED_SIM_MODES`를 엔드포인트에서 직접 사용하면 됨 — 별도 변수 추가 없이 `if mode not in _ALLOWED_SIM_MODES:` 로 작성.

- [ ] **Step 6: auth 미들웨어에 `/api/yard-sim` 화이트리스트 추가**

`backend/main.py`에서 137~138번째 줄 근처:

```python
            if path == "/api/schedule":
                return await call_next(request)
            if path == "/api/banchu":
                return await call_next(request)
```

다음으로 교체:

```python
            if path == "/api/schedule":
                return await call_next(request)
            if path == "/api/banchu":
                return await call_next(request)
            if path == "/api/yard-sim":
                return await call_next(request)
```

- [ ] **Step 7: 테스트 통과 확인**

```bash
cd /Users/jo/Desktop/프로젝트
python3 -m pytest tests/test_yard_sim_api.py -v
```

Expected: 5개 모두 PASS

- [ ] **Step 8: 전체 테스트 통과 확인**

```bash
python3 -m pytest tests/ -v 2>&1 | tail -5
```

Expected: `XX passed`

- [ ] **Step 9: 커밋**

```bash
git add backend/main.py tests/test_yard_sim_api.py
git commit -m "feat: add yard_sim_settings table and GET/PUT /api/yard-sim endpoints"
```

---

### Task 2: yard.html — 캐시 기반 DB 연동 + async IIFE 초기화

**Files:**
- Modify: `yard.html`

- [ ] **Step 1: 전역 캐시 변수 추가**

`yard.html`에서 `const DEFAULT_SETTINGS = {` 블록 바로 위(512번째 줄 근처, `const BERTH_KEY` 아래)에 추가:

```javascript
// DB 캐시 (async IIFE에서 채움)
let _scheduleCache = [];
let _simCache = { overall: null, import: null };
```

- [ ] **Step 2: `loadSimSettings` 함수 교체**

`yard.html`에서 아래 함수 전체를 찾아:

```javascript
function loadSimSettings(mode = getPageMode()) {
  try {
    const key = simStorageKeyByMode(mode);
    const raw = localStorage.getItem(key);
    const legacyRaw = localStorage.getItem(LEGACY_SIM_KEY);
    if (!raw && legacyRaw && mode === 'overall') {
      localStorage.setItem(key, legacyRaw);
    }
    const effectiveRaw = raw || (mode === 'overall' ? legacyRaw : null);
    if (!effectiveRaw) return JSON.parse(JSON.stringify(DEFAULT_SETTINGS));
    const parsed = JSON.parse(effectiveRaw);
    const d = DEFAULT_SETTINGS;
    return {
      selfYard: {
        ...d.selfYard,
        ...parsed.selfYard,
        outFixed: {
          ...d.selfYard.outFixed,
          ...(parsed.selfYard?.outFixed || {}),
          seaPlans: Array.isArray(parsed.selfYard?.outFixed?.seaPlans)
            ? parsed.selfYard.outFixed.seaPlans
            : [{ ton: Number(parsed.selfYard?.outFixed?.sea || 0) || 0, cycleDays: 1 }],
          silo: Number(parsed.selfYard?.outFixed?.silo ?? parsed.selfYard?.outFixed?.sea ?? d.selfYard.outFixed.silo) || 0,
          cts: Number(parsed.selfYard?.outFixed?.cts ?? d.selfYard.outFixed.cts) || 0,
          pTransfer: Number(parsed.selfYard?.outFixed?.pTransfer ?? d.selfYard.outFixed.pTransfer) || 0,
        },
        outDaily: parsed.selfYard?.outDaily || {},
      },
      rentYard: {
        ...d.rentYard,
        ...parsed.rentYard,
        outFixed: {
          ...d.rentYard.outFixed,
          ...(parsed.rentYard?.outFixed || {}),
          seaPlans: Array.isArray(parsed.rentYard?.outFixed?.seaPlans)
            ? parsed.rentYard.outFixed.seaPlans
            : [{ ton: Number(parsed.rentYard?.outFixed?.sea || 0) || 0, cycleDays: 1 }],
          silo: Number(parsed.rentYard?.outFixed?.silo ?? parsed.rentYard?.outFixed?.sea ?? d.rentYard.outFixed.silo) || 0,
          cts: Number(parsed.rentYard?.outFixed?.cts ?? d.rentYard.outFixed.cts) || 0,
          pTransfer: Number(parsed.rentYard?.outFixed?.pTransfer ?? d.rentYard.outFixed.pTransfer) || 0,
        },
        outDaily: parsed.rentYard?.outDaily || {},
      },
      silo:          { ...d.silo,          ...parsed.silo },
      autoInbound:   parsed.autoInbound   || {},
      manualInbound: parsed.manualInbound || {},
      manualOutbound: parsed.manualOutbound || {},
      manualOutboundDetail: parsed.manualOutboundDetail || {},
      lastFetched:   parsed.lastFetched   || null,
    };
  } catch { return JSON.parse(JSON.stringify(DEFAULT_SETTINGS)); }
}
```

다음으로 교체 (캐시에서 읽되, 파싱/병합 로직 유지):

```javascript
function loadSimSettings(mode = getPageMode()) {
  try {
    const parsed = _simCache[mode];
    if (!parsed) return JSON.parse(JSON.stringify(DEFAULT_SETTINGS));
    const d = DEFAULT_SETTINGS;
    return {
      selfYard: {
        ...d.selfYard,
        ...parsed.selfYard,
        outFixed: {
          ...d.selfYard.outFixed,
          ...(parsed.selfYard?.outFixed || {}),
          seaPlans: Array.isArray(parsed.selfYard?.outFixed?.seaPlans)
            ? parsed.selfYard.outFixed.seaPlans
            : [{ ton: Number(parsed.selfYard?.outFixed?.sea || 0) || 0, cycleDays: 1 }],
          silo: Number(parsed.selfYard?.outFixed?.silo ?? parsed.selfYard?.outFixed?.sea ?? d.selfYard.outFixed.silo) || 0,
          cts: Number(parsed.selfYard?.outFixed?.cts ?? d.selfYard.outFixed.cts) || 0,
          pTransfer: Number(parsed.selfYard?.outFixed?.pTransfer ?? d.selfYard.outFixed.pTransfer) || 0,
        },
        outDaily: parsed.selfYard?.outDaily || {},
      },
      rentYard: {
        ...d.rentYard,
        ...parsed.rentYard,
        outFixed: {
          ...d.rentYard.outFixed,
          ...(parsed.rentYard?.outFixed || {}),
          seaPlans: Array.isArray(parsed.rentYard?.outFixed?.seaPlans)
            ? parsed.rentYard.outFixed.seaPlans
            : [{ ton: Number(parsed.rentYard?.outFixed?.sea || 0) || 0, cycleDays: 1 }],
          silo: Number(parsed.rentYard?.outFixed?.silo ?? parsed.rentYard?.outFixed?.sea ?? d.rentYard.outFixed.silo) || 0,
          cts: Number(parsed.rentYard?.outFixed?.cts ?? d.rentYard.outFixed.cts) || 0,
          pTransfer: Number(parsed.rentYard?.outFixed?.pTransfer ?? d.rentYard.outFixed.pTransfer) || 0,
        },
        outDaily: parsed.rentYard?.outDaily || {},
      },
      silo:          { ...d.silo,          ...parsed.silo },
      autoInbound:   parsed.autoInbound   || {},
      manualInbound: parsed.manualInbound || {},
      manualOutbound: parsed.manualOutbound || {},
      manualOutboundDetail: parsed.manualOutboundDetail || {},
      lastFetched:   parsed.lastFetched   || null,
    };
  } catch { return JSON.parse(JSON.stringify(DEFAULT_SETTINGS)); }
}
```

- [ ] **Step 3: `saveSimSettings` 함수 교체**

`yard.html`에서 아래를 찾아:

```javascript
function saveSimSettings(s) {
  localStorage.setItem(simStorageKeyByMode(getPageMode()), JSON.stringify(s));
}
```

다음으로 교체:

```javascript
function saveSimSettings(s) {
  const mode = getPageMode();
  _simCache[mode] = s;
  fetch(`/api/yard-sim?mode=${mode}`, {
    method: 'PUT',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(s),
  }).catch(err => console.warn('시뮬 설정 저장 실패:', err));
}
```

- [ ] **Step 4: `let SIM = loadSimSettings()` 줄 제거**

`yard.html`에서 603번째 줄 근처:

```javascript
let SIM = loadSimSettings();
```

이 줄을 다음으로 교체 (async IIFE에서 설정하므로 선언만 유지):

```javascript
let SIM = JSON.parse(JSON.stringify(DEFAULT_SETTINGS));
```

- [ ] **Step 5: `calcAutoInbound` 함수에서 `_scheduleCache` 사용**

`yard.html`에서 `calcAutoInbound` 함수 안의 첫 줄을 찾아:

```javascript
  const raw = JSON.parse(localStorage.getItem(BERTH_KEY) || '[]');
```

다음으로 교체:

```javascript
  const raw = _scheduleCache;
```

- [ ] **Step 6: INIT 섹션 async IIFE로 교체**

`yard.html` 맨 아래 INIT 섹션을 찾아:

```javascript
// ─────────────────────────────────────────
//  INIT
// ─────────────────────────────────────────
document.getElementById('pageSelect').value = getPageMode();
refresh();
```

다음으로 교체:

```javascript
// ─────────────────────────────────────────
//  INIT (async — DB에서 로드)
// ─────────────────────────────────────────
(async function init() {
  try {
    const [overallRes, importRes, scheduleRes] = await Promise.all([
      fetch('/api/yard-sim?mode=overall'),
      fetch('/api/yard-sim?mode=import'),
      fetch('/api/schedule'),
    ]);
    if (overallRes.ok) {
      const d = await overallRes.json();
      if (d) _simCache.overall = d;
    }
    if (importRes.ok) {
      const d = await importRes.json();
      if (d) _simCache.import = d;
    }
    if (scheduleRes.ok) {
      _scheduleCache = await scheduleRes.json();
    }
  } catch (e) {
    console.warn('초기 데이터 로드 실패:', e);
  }
  SIM = loadSimSettings(getPageMode());
  document.getElementById('pageSelect').value = getPageMode();
  refresh();
})();
```

- [ ] **Step 7: 동작 확인 (로컬)**

```bash
cd /Users/jo/Desktop/프로젝트
python3 -m uvicorn backend.main:app --reload --port 8000
```

브라우저에서 `http://localhost:8000/yard` 열기:
1. 시뮬 설정 변경 → Network 탭에서 `PUT /api/yard-sim?mode=overall` 호출 확인
2. 페이지 새로고침 → 설정이 유지되는지 확인
3. 다른 탭에서 같은 URL 열어 설정이 공유되는지 확인
4. `http://localhost:8000/schedule` 에서 선박 등록 후 yard 페이지에서 자동 반입 계획 불러오기 동작 확인

- [ ] **Step 8: 커밋**

```bash
git add yard.html
git commit -m "feat: yard.html localStorage → /api/yard-sim DB 연동 + _scheduleCache 반입 계획 연동"
```

---

### Task 3: Push 및 배포 확인

**Files:** 없음 (git push)

- [ ] **Step 1: 전체 테스트 최종 확인**

```bash
cd /Users/jo/Desktop/프로젝트
python3 -m pytest tests/ -v 2>&1 | tail -5
```

Expected: `XX passed`

- [ ] **Step 2: Push**

```bash
git push origin main
```

Git 푸시 후 연결된 호스팅(Railway 등)에서 자동 빌드·배포되는지 확인합니다.

- [ ] **Step 3: 배포 후 검증**

1. 배포된 공개 URL의 `/yard` 에서 시뮬 설정 변경
2. 다른 기기/브라우저에서 같은 URL 열어 설정이 유지되는지 확인
3. schedule 페이지에서 선박 등록 후 yard 페이지 자동 반입 계획 불러오기 정상 동작 확인
4. 서버 `data/yard_sim_overall.json`, `data/yard_sim_import.json` 파일이 갱신되는지 확인
